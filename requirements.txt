# Dashboard dependencies — this is the file Streamlit Cloud installs.
# Bot-only extras (Telegram, weather) live in requirements-bot.txt.
# Local full setup (bot + dashboard):  pip install -r requirements-bot.txt

# streamlit-float 0.4.0 requires streamlit>=1.45, so don't pin below that.
streamlit>=1.45
streamlit-float==0.4.0
pandas==2.2.3
plotly==5.24.1

# Supabase client (used by dashboard + bot). Pulls a compatible httpx itself —
# we deliberately don't pin httpx here to avoid a resolver conflict.
supabase==2.9.1

# AI brain (coach chat + vision). Reused by the dashboard's coach.
anthropic==0.39.0

# Dashboard login password hashing
bcrypt==4.2.1

# Config + secrets
python-dotenv==1.0.1
